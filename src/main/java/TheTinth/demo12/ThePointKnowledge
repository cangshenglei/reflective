 * this和super关键字的注意事项:
 *      1.在静态成员(方法,代码块,成员内部类)中无法使用this和super关键字
 *      2.this(实参)和super(实参)在构造器中必须编译在第一行
 *      3.this(实参)和super(实参)不能同时使用
 *      4.程序中的构造器执行分三个阶段:
 *          (1)显式或隐式的super(实参);
 *          (2)隐式部分代码(实例初始化过程)
 *          (3)显式部分代码