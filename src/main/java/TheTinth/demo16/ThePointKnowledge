权限访问级别补充:
 *      如果修饰类文件(class,interface,enum):
 *          权限访问级别:public和缺省,推荐public
 *      如果修饰类(class)中成员常量
 *          权限访问级别:四种都可以,推荐private
 *      如果修饰类(class)中成员变量
 *          权限访问级别:四种都可以,推荐private
 *      如果修饰类(class)中成员方法
 *          权限访问级别:四种都可以,推荐public和private
 *      如果修饰类(class)中成员内部类
 *          权限访问级别:四种都可以,推荐private
 *      如果修饰类(class)中构造器
 *          权限访问级别:四种都可以,推荐public和private
 *      如果修饰类(class)中构造器代码块
 *          权限访问级别:只能缺省
 *      如果修饰类(class)中静态代码块
 *          权限访问级别:只能缺省
 *      如果修饰接口(interface)中的静态常量
 *          权限访问级别:只能public,无论显式和隐式都是public
 *      如果修饰接口(interface)中的抽象方法
 *          权限访问级别:只能public,无论显式和隐式都是public
 *      如果修饰接口(interface)中的默认方法
 *          权限访问级别:只能public,无论显式和隐式都是public
 *      如果修饰接口(interface)中的静态方法
 *          权限访问级别:只能public,无论显式和隐式都是public
 *      如果修饰接口(interface)中的私有方法
 *          权限访问级别:只能private
 *      如果修饰接口(interface)中的内部接口
 *          权限访问级别:只能public,无论显式和隐式都是public
 *      如果修饰枚举类(enum)中的枚举对象:
 *          权限访问级别:只能隐式public,如果显式编译报错
 *      如果修饰枚举类(enum)中的成员变量:
 *          权限访问级别:四种都可以,推荐private
 *      如果修饰枚举类(enum)中的成员方法:
 *          权限访问级别:四种都可以,推荐public和private
 *      如果修饰枚举类(enum)中的构造器:
 *          权限访问级别:只能private,无论显式和隐式都private
 *      如果修饰局部中的常量
 *          权限访问级别:只能缺省
 *      如果修饰局部中的方法
 *          权限访问级别:只能缺省
 *      如果修饰局部中的内部类
 *          权限访问级别:只能缺省
 *
 * protected关键字的修饰场景:
 *      1.有继承关系的源码中
 *      2.封装带有继承关系的工具类时
		*      3.编写框架的源码
		*/