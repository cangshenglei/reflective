  链接列表(链表):
  *      类似于女生带的"手链",本质上其实就是一个类
  *
  * 链表的分类:
  *      单向链表:
  *          当前元素(泛型类型)
  *          下一个链表对象(链表类型)
  *      双向链表
  *          当前元素(泛型类型)
  *          下一个链表对象(链表类型)
  *          上一个链表对象(链表类型)
  *
  * 链表结构的特点:
  *      查询效率低,增删效率高
  *      针对链表结构中首尾两个元素快速操作
  *
  * 链表结构的弊端:
  *      链表越长且数据越靠后,链表结构的查询和增删效率会更低
  *
  * LinkedList类
  *      类的位置
  *          java.util
  *      类的特点
  *          1.LinkedList集合底层的数据结构是"链表结构",而且是双向链表
  *          2.LinkedList集合可以存储null元素,在获取元素时候需要进行非空校验,防止空指针异常
  *          3.LinkedList集合是线程不安全的,适用于单线程程序,如果在多线程中进行使用,需要手动添加线程安全处理
  *      类的构造器
  *          public LinkedList()
  *              构造一个空列表。
  *      类的方法
  *          public void addFirst(E e)
  *              将指定元素插入此列表的开头。
  *          public void addLast(E e)
  *              将指定元素添加到此列表的结尾。
  *          public E getFirst()
  *              返回此列表的第一个元素。
  *          public E getLast()
  *              返回此列表的最后一个元素。
  *          public E removeFirst()
  *              移除并返回此列表的第一个元素。
  *          public E removeLast()
  *              移除并返回此列表的最后一个元素。