多态的前提条件:
 *      1.必须要有"继承关系"或者"实现关系"
 *      2.必须要有方法的重写(没有方法重写在格式上也可以构成多态,只不过这样多态没有任何意义)
 *      3.如果是继承关系,父类的引用指向子类对象(父类new子类)
 *        如果是实现关系,父接口的引用指向实现类对象(父接口new实现类)
 *        多态的特殊形式:构造器中的super();(暂不涉及)