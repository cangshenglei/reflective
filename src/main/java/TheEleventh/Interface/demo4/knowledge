接口中的默认方法
 *      含义:
 *          所谓默认方法,其实就是原来父类中抽取多个子类相同且不需要重写的实例方法,考虑到接口不能创建对象,需要将该方法改写为默认形式
 *      格式:
 *          [public] default 返回类型 方法名 () {}
 *      注意事项:
 *          格式中的[]代表关键字可以省略不写,如果省略不写,由JVM自动填充补全
 *      应用场景:
 *          1.源码中
 *          2.笔试题
 *