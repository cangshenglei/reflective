接口中的静态常量:
 *      格式:
 *          [public] [static] [final] 数据类型 常量名 = 常量值;
 *      注意事项:
 *          1.格式中的[]代表关键字可以省略不写,如果省略不写,由JVM自动填充补全
 *          2.接口中的静态常量不可以先声明后初始化
 *      调用格式:
 *          接口名.静态常量名;